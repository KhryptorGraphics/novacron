# NovaCron AI Operations Engine — full environment
#
# Rich/dev environment (containers, CI, workstations with a full toolchain).
# Composed from the split files so the same pin set can also be installed in
# pieces:
#
#   requirements-core.txt  API boot + serve subset (wheel-only on py3.13/arm64)
#   requirements-ml.txt    heavy training/monitoring science stack
#   requirements-dev.txt   test/lint/auth tooling
#
# For a native boot on aarch64 without a build toolchain, install only the core:
#   pip install -r requirements-core.txt

-r requirements-core.txt
-r requirements-ml.txt
-r requirements-dev.txt