# NovaCron AI Operations Engine — CORE runtime (boot subset)
#
# Everything the API process (ai_engine.api.main:app) needs to import, start and
# serve /health, /metrics and the /api/v1/* prediction endpoints from a
# wheel-only install on CPython 3.13 / linux-aarch64.
#
# Nothing here is imported by tensorflow / pyod / tsfresh — those live in
# requirements-ml.txt and are imported lazily by ai_engine.core.* so the API can
# boot without the heavy science stack.
#
#   core (this file)  +  requirements-ml.txt  +  requirements-dev.txt  =  requirements.txt
#
# Lower bounds below are the lowest releases that ship cp313 + manylinux aarch64
# wheels (verified against the PyPI index for this interpreter).

# --- Model inference (used in-process by the prediction endpoints) ---
numpy>=2.1.0,<3.0.0          # FIX(novacron-sdu): was >=1.24.0,<2.0.0 — no numpy<2.1.0 release has cp313 wheels
pandas>=2.2.3,<3.0.0         # FIX: was >=2.0.0 — 2.2.3 is the lowest release with cp313/aarch64 wheels
scikit-learn>=1.5.2,<2.0.0   # FIX: was >=1.3.0 — 1.5.2 is the lowest release with cp313/aarch64 wheels
scipy>=1.14.1,<2.0.0         # FIX: was >=1.10.0 — 1.14.1 is the lowest release with cp313/aarch64 wheels
xgboost>=2.1.0,<3.0.0        # 2.1.0 ships a CPython 3.13 / linux-aarch64 wheel.
lightgbm>=4.6.0,<5.0.0       # FIX: was >=3.3.0,<4.0.0 — LightGBM <4.6 calls
                             # sklearn.utils.validation.check_X_y(force_all_finite=...), which
                             # scikit-learn removed (renamed ensure_all_finite) in 1.6, so every
                             # resolvable version of the old range raises
                             # "TypeError: check_X_y() got an unexpected keyword argument
                             # 'force_all_finite'" on the first .fit(). 4.6.0 is the lowest release
                             # that fits and predicts against scikit-learn >=1.6 (verified: 4.5.0
                             # fails, 4.6.0 passes on scikit-learn 1.9.1).

# --- API framework & async ---
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.22.0,<1.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
httpx>=0.24.0,<1.0.0

# --- Database & cache ---
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.11.0,<2.0.0
redis>=4.5.0,<5.0.0
# aioredis>=2.0.0,<3.0.0 was dropped (novacron-sdu): every aioredis release does
# `from distutils.version import StrictVersion`, and distutils was removed from
# the stdlib in CPython 3.12, so 2.0.1 — the final release of the now-archived
# project — dies with ModuleNotFoundError on this interpreter. redis>=4.2 ships
# the supported replacement (redis.asyncio), and nothing in ai_engine imports
# aioredis.
psycopg2-binary>=2.9.10,<3.0.0   # FIX: was >=2.9.0 — 2.9.10 is the lowest release with cp313/aarch64 wheels

# --- Monitoring & logging ---
prometheus-client>=0.17.0,<1.0.0
structlog>=23.0.0,<24.0.0
python-json-logger>=2.0.0,<3.0.0

# --- Serialization & model persistence ---
joblib>=1.3.0,<2.0.0
dill>=0.3.0,<1.0.0
cloudpickle>=2.2.0,<3.0.0