phase: 4
sprint: 8
bead: novacron-j1s
status: in_progress
slice: migration-backup-live-contracts
completed:
  - Added canonical authenticated migration and backup contract routes under /api and /api/v1.
  - Replaced VMMigrationDialog fixture behavior with a typed live API submission path.
  - Covered migration and backup contract routes in api-server tests.
  - Replaced DatabaseEditor fixture CRUD with canonical admin user API hooks.
  - Replaced NetworkConfigurationPanel fixture data with live virtual-network list, create, and delete operations.
validation:
  - go test ./backend/cmd/api-server
  - frontend npm run test:smoke -- --runInBand
  - frontend npx tsc --noEmit --pretty false
remaining:
  - Add canonical firewall and load-balancer APIs before enabling those NetworkConfigurationPanel tabs.
  - Replace MigrationWorkflow and BackupRecoveryFlow demos with live planning, execution, restore, and policy contracts.
